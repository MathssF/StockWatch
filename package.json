{
  "name": "stockwatch",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "producer",
    "consumer",
    "shared"
  ],
  "description": "",
  "main": "index.js",
  "scripts": {
    "start:producer": "npm start --workspace=producer",
    "start:consumer": "npm start --workspace=consumer",
    "build": "npm run build --workspaces",
    "prisma:generate": "prisma generate --schema=shared/src/prisma/schema.prisma",
    "prisma:migrate": "prisma migrate dev --schema=shared/src/prisma/schema.prisma",
    "prisma:studio": "prisma studio --schema=shared/prisma/src/schema.prisma",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.17.1",
    "amqplib": "^0.8.0",
    "dotenv": "^16.0.0",
    "mongodb": "^5.0.0",
    "mysql2": "^2.3.0"
  },
  "devDependencies": {
    "typescript": "^4.5.0",
    "prisma": "^3.0.0",
    "nodemon": "^2.0.15"
  },
  "keywords": [],
  "author": "",
  "license": "ISC"
}
